services:
  - type: cron
    name: arbitrage-detector
    schedule: "*/5 * * * *"  # Every 5 minutes
    runtime: python
    plan: free
    buildCommand: ""
    startCommand: "python main.py"
    envVars:
      - key: ODDS_API_KEY
        sync: false
      - key: DISCORD_WEBHOOK_URL
        sync: false

